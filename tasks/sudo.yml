- name: Install sudo
  pacman:
    packages: sudo

- name: Copy sudoers
  template:
    src: files/sudoers
    dest: /home/naya/Downloads/etc/sudoers
    mode: 0440
    validate: /usr/sbin/visudo -csf %s

- name: Create sudo Group
  group: sudo
